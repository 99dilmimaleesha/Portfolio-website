const names = ["alice" , "bob", "charlie"];

const namesupper = names.map(firtsletterUpper);

console.log(namesupper);

function firtsletterUpper(element){
     return element.charAt(upperfirstLetter);
}
